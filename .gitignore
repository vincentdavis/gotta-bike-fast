# Godot 4+ specific ignores
.godot/
/android/

# export_presets.cfg IS committed — CI (GitHub Actions) needs it to export,
# and it carries no secrets (just preset names, icon paths, bundle id).
# Signing certs / passwords live in export_credentials.cfg, which stays
# ignored.
export_credentials.cfg

# macOS
.DS_Store

# IDE
.idea/
.vscode/

# Build / export output
build/
dist_pkg/
