# Keep the Docker build context lean — only the Godot project sources are
# needed to export the Web build.
.git
.github
build/
.godot/
bridge/
tools/
android/
*.md
export_credentials.cfg
.DS_Store
# Blender working files (untracked, not game assets). The image has no
# Blender, and a .blend here made the headless import skip everything.
art/
